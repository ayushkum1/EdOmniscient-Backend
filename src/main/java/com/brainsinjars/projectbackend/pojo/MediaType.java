package com.brainsinjars.projectbackend.pojo;

/**
 * A media can be an image or a video.
 * This enum ensures type safety
 * for saving type of media in DB
 */
public enum MediaType {
    IMAGE, VIDEO
}
